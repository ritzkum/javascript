function getrandom() {
    //   const randomValue = circle[Math.floor(Math.random() * circle.length)];
    //   document.querySelector(".content").textContent = circle;
      document.write(circle);
    }
    
    const circle = document.getElementsByClassName("").innerHTML;